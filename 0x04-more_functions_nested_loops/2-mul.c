#include "main.h"

/**
 * nul - mutliplies two integer
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: result of multiplication of two numbers
 */
int nul(int x, int y)
{
	return (x * y);
}
